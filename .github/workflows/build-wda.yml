name: Build WDA.ipa

on:
  workflow_dispatch:
    inputs:
      wda_version:
        description: 'WebDriverAgent version tag (e.g. v11.4.3)'
        required: true
        default: 'v11.4.3'
      add_adhoc_sign:
        description: 'Add ad-hoc signature (not needed for Sideloadly)'
        required: true
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

jobs:
  build:
    runs-on: macos-14
    steps:
      - name: Checkout WDA
        uses: actions/checkout@v4
        with:
          repository: appium/WebDriverAgent
          ref: ${{ github.event.inputs.wda_version }}

      - name: Select Xcode
        run: |
          sudo xcode-select -s /Applications/Xcode_15.4.app
          xcodebuild -version

      - name: Build for testing (unsigned)
        run: |
          xcodebuild build-for-testing \
            -project WebDriverAgent.xcodeproj \
            -scheme WebDriverAgentRunner \
            -destination 'generic/platform=iOS' \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            DEVELOPMENT_TEAM=""

      - name: Create IPA (unsigned)
        if: github.event.inputs.add_adhoc_sign == 'false'
        run: |
          mkdir -p Payload
          cp -r build/Build/Products/Release-iphoneos/WebDriverAgentRunner-Runner.app Payload/
          zip -r WebDriverAgent.ipa Payload/
          echo "=== IPA contents ==="
          unzip -l WebDriverAgent.ipa | head -30

      - name: Add ad-hoc signature to all binaries
        if: github.event.inputs.add_adhoc_sign == 'true'
        run: |
          APP_DIR="build/Build/Products/Release-iphoneos/WebDriverAgentRunner-Runner.app"
          
          # Find and sign all Mach-O binaries
          for binary in $(find "$APP_DIR" -type f -exec sh -c 'file "$1" | grep -q "Mach-O" && echo "$1"' _ {} \;); do
            echo "Signing: $binary"
            codesign --force --sign - --timestamp=none "$binary"
          done
          
          # Create IPA
          mkdir -p Payload
          cp -r "$APP_DIR" Payload/
          zip -r WebDriverAgent.ipa Payload/

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: WebDriverAgent-ipa-${{ github.event.inputs.wda_version }}
          path: WebDriverAgent.ipa
